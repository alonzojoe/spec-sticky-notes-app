import { Board } from '@/components/board/board'

/** The linked section: the same board, drawing only the notes that carry a link. */
export function LinkedPage() {
  return <Board section="linked" />
}
